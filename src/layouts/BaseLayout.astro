---
export interface Props {
  title: string;
  description: string;
  canonical?: string;
  image?: string;
  pageSchema?: any;
}

const { title, description, canonical, image, pageSchema } = Astro.props;

// Site constants
const siteUrl = "https://wrongfulterminationcalc.com";
const siteName = "WrongfulTerminationCalc";
const brandName = "WrongfulTerminationCalc";
const publisherName = "The Click Lab Agency LLC";
const publisherUrl = "https://theclicklab.agency";

// Editor / reviewer — matches the generic editorial-team bylines on methodology and editorial pages
const editorInChief = {
  name: "Editorial Team",
  credentials: "",
  role: "Editorial Team",
  bio: "WrongfulTerminationCalc content is reviewed by our editorial team, which maintains methodology and editorial standards.",
  url: `${siteUrl}/editorial/`,
};

const canonicalUrl = canonical || new URL(Astro.url.pathname, siteUrl).toString();
const ogImage = image ? new URL(image, siteUrl).toString() : new URL('/og-default.png', siteUrl).toString();

const organizationSchema = {
  '@type': 'Organization',
  '@id': `${publisherUrl}/#organization`,
  name: publisherName,
  url: publisherUrl,
  legalName: publisherName,
};

const personSchema = {
  '@type': 'Person',
  '@id': `${siteUrl}/#editor-in-chief`,
  name: editorInChief.name,
  honorificSuffix: editorInChief.credentials,
  jobTitle: editorInChief.role,
  description: editorInChief.bio,
  url: editorInChief.url,
  worksFor: { '@id': `${publisherUrl}/#organization` },
};

const websiteSchema = {
  '@type': 'WebSite',
  '@id': `${siteUrl}/#website`,
  url: siteUrl,
  name: siteName,
  alternateName: brandName,
  publisher: { '@id': `${publisherUrl}/#organization` },
  inLanguage: 'en-US',
};

const siteGraph = {
  '@context': 'https://schema.org',
  '@graph': [organizationSchema, personSchema, websiteSchema],
};
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:type" content="website" />
    <meta property="og:image" content={ogImage} />
    <meta property="og:site_name" content={siteName} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />
    <script type="application/ld+json" set:html={JSON.stringify(siteGraph)}></script>
    {pageSchema && (
      <script type="application/ld+json" set:html={JSON.stringify(pageSchema)}></script>
    )}
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-6434377544325972" crossorigin="anonymous"></script>
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-VRDRZSY4XV"></script>
    <script>window.dataLayer=window.dataLayer||[];function gtag(){dataLayer.push(arguments);}gtag('js',new Date());gtag('config','G-VRDRZSY4XV');</script>
  </head>
<body>
    <header class="site-header">
      <div class="container">
        <a href="/" class="site-logo">WrongfulTerminationCalc</a>
        <nav class="site-nav" aria-label="Primary">
          <a href="/">Calculator</a>
          <a href="/case-types/">Claim Types</a>
          <a href="/guides/">Guides</a>
          <a href="/about/">About</a>
        </nav>
      </div>
    </header>

    <main class="site-main">
      <div class="container">
        <slot />
      </div>
    </main>

    <footer class="site-footer">
      <div class="container">
        <nav class="footer-nav" aria-label="Footer">
          <a href="/about/">About</a>
          <a href="/methodology/">Methodology</a>
          <a href="/editorial/">Editorial Team</a>
          <a href="/contact/">Contact</a>
          <a href="/faq/">FAQ</a>
          <a href="/privacy/">Privacy</a>
          <a href="/terms/">Terms</a>
        </nav>
        <p class="footer-credit">&copy; 2026 The Click Lab Agency LLC. Informational only, not legal advice. Consult an employment attorney or the EEOC about your situation.</p>
      </div>
    </footer>

    <style is:global>
      :root {
        --color-bg: #ffffff;
        --color-text: #1a1a1a;
        --color-muted: #555;
        --color-accent: #0a66c2;
        --color-border: #e0e0e0;
        --font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
        --max-width: 760px;
      }
      * { box-sizing: border-box; }
      body { margin: 0; font-family: var(--font-sans); line-height: 1.6; color: var(--color-text); background: var(--color-bg); }
      .container { max-width: var(--max-width); margin: 0 auto; padding: 0 1rem; }
      .site-header { border-bottom: 1px solid var(--color-border); padding: 1rem 0; }
      .site-header .container { display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 1rem; }
      .site-logo { font-weight: 700; font-size: 1.25rem; color: var(--color-text); text-decoration: none; }
      .site-nav { display: flex; gap: 1.25rem; flex-wrap: wrap; }
      .site-nav a { color: var(--color-text); text-decoration: none; font-size: 0.95rem; }
      .site-nav a:hover { color: var(--color-accent); }
      .site-main { padding: 2rem 0 4rem; }
      h1, h2, h3 { line-height: 1.25; }
      h1 { font-size: 2rem; margin: 0 0 0.5rem; }
      h2 { font-size: 1.5rem; margin: 2rem 0 0.75rem; }
      h3 { font-size: 1.2rem; margin: 1.5rem 0 0.5rem; }
      a { color: var(--color-accent); }
      .site-footer { border-top: 1px solid var(--color-border); padding: 2rem 0; margin-top: 4rem; color: var(--color-muted); font-size: 0.9rem; }
      .footer-nav { display: flex; gap: 1.25rem; flex-wrap: wrap; margin-bottom: 0.75rem; }
      .footer-nav a { color: var(--color-muted); text-decoration: none; }
      .footer-nav a:hover { color: var(--color-accent); }
      .footer-credit { margin: 0; }
      form { background: #f7f7f7; border: 1px solid var(--color-border); border-radius: 6px; padding: 1.5rem; margin: 1rem 0 2rem; }
      label { display: block; margin: 1rem 0 0.25rem; font-weight: 600; }
      input, select { width: 100%; padding: 0.5rem; font-size: 1rem; border: 1px solid var(--color-border); border-radius: 4px; }
      button[type="submit"] { margin-top: 1.5rem; padding: 0.75rem 1.5rem; background: var(--color-accent); color: white; border: none; border-radius: 4px; font-size: 1rem; font-weight: 600; cursor: pointer; }
      button[type="submit"]:hover { opacity: 0.9; }
    </style>
  </body>
</html>